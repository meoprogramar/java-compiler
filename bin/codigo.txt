package teste;    

import java.util.Scanner;

public class teste {
	int inteiro;
	int resultado;
	private float flutuante;	
	protected boolean booleano = true;
	public static final String CONST = "String teste";
	
	public testando(int inteiro, int flutuante){
		inteiro = inteiro;
		flutuante = flutuante;
	}
	
	private int antecessor(int valor){
		if(valor <= 1){
			return 1;
		} else {
			return antecessor(valor-1);
		}
	}
	
	private int predecessor(int valor){
		if(valor <= 1){
			return 1;
		} else {
			return predecessor(valor+1);
		}
	}
	
	public int somarValores(int a, int b){
		return (a+b);
	}
	
	public int subtrairValores(int a, int b){
		return (a-b);
	}
	
	public int multiplicarValores(int a, int b){
		return (a*b);
	}
	
	public void exibirMensagem(String mensagem){
		System.out.println(mensagem);
	}
	
	public static void main(String[] args){
		boolean booleano = true;
		
		exibirMensagem("Teste");
		
		int soma = somandoValores(123, 33);
		System.out.println("O resultado é: "+soma);
		
		int subtracao = subtrairValores(123, 33);
		System.out.println("O resultado é: "+subtracao);
		
		int multiplicacao = multiplicarValores(123, 33);
		System.out.println("O resultado é: "+multiplicacao);
		
		System.out.println("Antecessor de 100 é "+antecessor(100));
		
		System.out.println("Predecessor de 100 é "+predecessor(100));
		
		if(booleano){
			if(soma < 10){
				System.out.println("O resultado da soma é maior que 10!");
			} else {
				if(soma++ == 10){				
					System.out.println("O resultado da soma é igual a 10!");
					booleano = !booleano;
				}
			}
		} else {
			while(soma = 20 && b < 5){
				if(booleano && (soma != 20)){
					return result += 1;
				}
			}
		}
	}	
}